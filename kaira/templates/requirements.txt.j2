# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — generated by Kaira.
# Canonical dependency source is pyproject.toml; this file mirrors it so the
# project installs on any host with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

fastapi[standard]>=0.115.0
uvicorn[standard]>=0.29.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
slowapi>=0.1.9
loguru>=0.7.2
python-dotenv>=1.0.1
bcrypt>=4.1.2,<5.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database ({{ db_type }})
{% if db_type == "postgresql" -%}
sqlalchemy[asyncio]>=2.0.36,<3.0.0
asyncpg>=0.30.0
alembic>=1.14.0
{% elif db_type == "mysql" -%}
sqlalchemy[asyncio]>=2.0.36,<3.0.0
aiomysql>=0.2.0
alembic>=1.14.0
{% elif db_type == "mongodb" -%}
motor>=3.3.0,<4.0.0
beanie>=1.24.0,<2.0.0
{% else -%}
sqlalchemy[asyncio]>=2.0.36,<3.0.0
aiosqlite>=0.20.0
alembic>=1.14.0
{% endif %}
