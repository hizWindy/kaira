"""{{ project_name }} — Auto-generated by Khaira (Simple Tier)."""

from __future__ import annotations

from kaira.app import KairaApp

app = KairaApp(
    project_name="{{ project_name }}",
    auto_register=True,
    enforce_layers=True,
)

# ── Simple tier uses KairaApp runtime ────────────
# For full framework features, use: kaira upgrade --tier standard
