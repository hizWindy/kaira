"""{{ project_name }} — Auto-generated by Khaira (Simple Tier)."""

from __future__ import annotations

from fastapi import FastAPI

app = FastAPI(
    title="{{ project_name }}",
    version="0.1.0",
)

# ── Simple tier uses FastAPI directly ────────────────────
# For full framework features, use: khaira upgrade --tier standard
