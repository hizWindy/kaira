"""
Seed script for {{ model_name }} (SQL / SQLAlchemy).
Auto-generated by Khaira.

Usage:
    python seeds/seed_{{ snake_name }}.py [--count N] [--force]
{% if has_password %}

Passwords are stored bcrypt-hashed. The plaintext for each seeded record is
printed after a successful run — see ``plain_password`` below.
{% endif %}
"""
{% import "_seed_macros.j2" as m %}
from __future__ import annotations

import argparse
import asyncio
{% if has_password and is_auth_model and login_identifier %}
import json
{% endif %}
import sys
from datetime import datetime, timedelta, timezone
from pathlib import Path

{% if needs_bcrypt %}
import bcrypt
{% endif %}
from sqlalchemy import func, select

# Seeds run as standalone scripts, so the project root must be importable.
PROJECT_ROOT = Path(__file__).resolve().parent.parent
if str(PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(PROJECT_ROOT))

from config.settings import settings
from core.database import AsyncSessionLocal
from {{ models_dir }}.{{ snake_name }} import {{ model_name }}

DEFAULT_COUNT = 5

# Fixed base instant keeps generated timestamps deterministic across runs.
BASE_TIME = datetime(2026, 1, 1, tzinfo=timezone.utc)


{% if has_password %}
{{ m.password_helper() }}


{% endif %}
def build_record(i: int) -> dict:
    """Build sample field values for the i-th {{ model_name }} row."""
    return {
{{ m.record_dict(fields, ' ' * 8, embedded_defs) }}
    }


async def seed(count: int = DEFAULT_COUNT, force: bool = False) -> int:
    """Insert sample {{ model_name }} rows into the database.

    Args:
        count: Number of rows to insert.
        force: Insert even when the table already holds rows.

    Returns:
        Number of rows inserted.

    Raises:
        RuntimeError: If APP_ENV is production.
    """
    if getattr(settings, "APP_ENV", "development") == "production":
        raise RuntimeError("Seeding is disabled in production")

    async with AsyncSessionLocal() as session:
        existing = await session.scalar(select(func.count()).select_from({{ model_name }}))
        if existing and not force:
            print(
                f"Skipped: '{{ table_name }}' already holds {existing} row(s). "
                "Re-run with --force to add more, or clear it with 'khaira seed clear'."
            )
            return 0

        session.add_all([{{ model_name }}(**build_record(i)) for i in range(1, count + 1)])
        await session.commit()

        total = await session.scalar(select(func.count()).select_from({{ model_name }}))
        print(f"Seeded {count} {{ model_name }} row(s); table now holds {total}.")
{% if has_password %}
{{ m.credentials_block(login_identifier, is_auth_model, auth_login_path, 8) }}
{% endif %}
        return count


def main() -> None:
    parser = argparse.ArgumentParser(description="Seed sample {{ model_name }} rows.")
    parser.add_argument("--count", type=int, default=DEFAULT_COUNT, help="How many rows to insert.")
    parser.add_argument("--force", action="store_true", help="Insert even if the table is non-empty.")
    args = parser.parse_args()

    # Errors propagate: a seed that inserted nothing must exit non-zero so the
    # caller never reports success for a failed run.
    asyncio.run(seed(count=args.count, force=args.force))


if __name__ == "__main__":
    main()
